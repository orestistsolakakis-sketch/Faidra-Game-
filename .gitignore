# Godot 4 + .NET

# Godot per-user editor data and import cache (regenerated on open).
.godot/
.import/
export.cfg
# NOTE: export_presets.cfg is intentionally committed — CI needs the Web preset.

# .NET build output.
.mono/
bin/
obj/
*.user

# OS / editor noise.
.DS_Store
Thumbs.db
*.swp
.idea/
.vs/

# Exported builds.
build/
*.zip
